const config = {
  baseURL: 'http://59.68.29.67:8000/api', // api的基地址
  timeout: 5000, // 响应的最大限制时间
  // 设置http的头部信息
  headers: { 'Content-Type': 'application/json;charset=UTF-8' }
}

export default config
